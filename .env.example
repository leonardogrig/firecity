# GitHub Personal Access Token (optional, recommended)
# Raises GitHub API rate limit from 60 to 5,000 requests/hour.
# No scopes needed — leave all checkboxes unchecked when creating.
# Get one at: https://github.com/settings/tokens
GITHUB_TOKEN=

# Firecrawl API Key (optional)
# Only needed for website branding, screenshots, and site info.
# Users can also provide their own key via the UI.
# Get one at: https://firecrawl.dev
FIRECRAWL_API_KEY=

# Redis URL (optional)
# Enables caching of GitHub/site data and per-IP rate limiting.
# Without Redis, every request hits the GitHub API directly.
# Example: redis://localhost:6379 or redis://user:password@host:port
REDIS_URL=

# Rate Limit Bypass (optional)
# Set to "true" to disable the 5-lookups-per-day per-IP limit.
# Useful during local development.
RATE_LIMIT_BYPASS=
